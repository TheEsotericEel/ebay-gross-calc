{
  "name": "ebay-gross-calc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build -- --watch",
    "clean": "rimraf dist",
    "build:ts": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"Remove-Item -Recurse -Force dist -ErrorAction Ignore; npx tsc -p tsconfig.sw.json; npx tsc -p tsconfig.content.json; New-Item -ItemType Directory -Force dist | Out-Null; Copy-Item manifest.json dist\\ -Force; Copy-Item public\\popup.html dist\\popup.html -Force; Copy-Item public\\options.html dist\\options.html -Force\""
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "chrome-types": "^0.1.372",
    "esbuild": "^0.25.9",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}